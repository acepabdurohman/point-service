spring:
  application:
    name: point-service
  datasource:
    url: jdbc:mysql://localhost:3306/db_point
    username: root
    password: admin1234
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
server:
  port: 9003